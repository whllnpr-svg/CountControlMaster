# Count Master 快速上手指南

## 🚀 5分钟快速开始

### 第一步：打开项目 (1分钟)

```bash
# 1. 确保已安装 Unity Hub
# 2. 在 Unity Hub 中添加项目
点击 "Add" → 选择 /home/user/CountControlMaster 目录

# 3. 确认 Unity 版本
Unity 2020.3.21f1 (如果没有，Unity Hub会提示安装)

# 4. 打开项目
双击项目名称打开
```

---

### 第二步：运行游戏 (1分钟)

```
1. 在 Project 窗口找到 Assets/Scenes/Menu.unity
2. 双击打开场景
3. 点击顶部的 ▶️ 播放按钮
4. 点击 "Start" 开始游戏
5. 拖拽鼠标左右移动
```

**控制方式：**
- 🖱️ 鼠标按住拖拽 = 左右移动
- 🚪 通过绿色门 = 增加队伍
- ⚔️ 碰到红色敌人 = 开始战斗

---

### 第三步：查看核心代码 (3分钟)

#### 最重要的3个文件：

##### 1. PlayerManager.cs（玩家控制）
```
位置：Assets/Scripts/PlayerManager.cs
作用：控制玩家移动、编队、战斗

关键方法：
- MoveThePlayer() → 触摸控制
- FormatStickMan() → 螺旋编队
- OnTriggerEnter() → 碰撞检测
- EnemyAttack() → 战斗逻辑
```

##### 2. GateManager.cs（门系统）
```
位置：Assets/Scripts/GateManager.cs
作用：管理增员门的数值

关键代码：
- multiply = true → 乘法门 (x1, x2)
- multiply = false → 加法门 (+10 到 +100)
```

##### 3. Tower.cs（塔楼构建）
```
位置：Assets/Scripts/Tower.cs
作用：胜利后建造金字塔

关键方法：
- BuildTower() → 计算层数并构建
```

---

## 🎯 第一个修改：改变编队形状

### 任务：修改队伍排列方式

**步骤：**

1. 打开 `PlayerManager.cs`
2. 找到 `FormatStickMan()` 方法（约第138行）
3. 找到这段代码：

```csharp
for (int i = 1; i < player.childCount; i++)
{
    var x = DistanceFactor * Mathf.Sqrt(i) * Mathf.Cos(i * Radius);
    var z = DistanceFactor * Mathf.Sqrt(i) * Mathf.Sin(i * Radius);
    var NewPos = new Vector3(x, -0.55f, z);
    player.transform.GetChild(i).DOLocalMove(NewPos, 0.5f).SetEase(Ease.OutBack);
}
```

4. 尝试以下修改：

#### 修改A：改变编队紧密度
```csharp
// 原代码：DistanceFactor = 1
// 修改为：
DistanceFactor = 0.5f;  // 队伍更紧密
// 或
DistanceFactor = 2f;    // 队伍更分散
```

#### 修改B：改变动画效果
```csharp
// 原代码：SetEase(Ease.OutBack)
// 修改为：
.SetEase(Ease.OutBounce)  // 弹跳效果
// 或
.SetEase(Ease.OutElastic) // 弹性效果
```

#### 修改C：改为圆形阵型
```csharp
// 替换整个循环为：
int rowSize = Mathf.CeilToInt(Mathf.Sqrt(player.childCount));
for (int i = 1; i < player.childCount; i++)
{
    float angle = (360f / 8f) * (i % 8) * Mathf.Deg2Rad;
    float radius = Mathf.Floor(i / 8f) * 0.5f;

    var x = Mathf.Cos(angle) * radius;
    var z = Mathf.Sin(angle) * radius;
    var NewPos = new Vector3(x, -0.55f, z);

    player.transform.GetChild(i).DOLocalMove(NewPos, 0.5f).SetEase(Ease.OutBack);
}
```

5. 保存文件（Ctrl+S）
6. 返回 Unity，等待编译
7. 点击播放测试效果

---

## 🎨 第二个修改：改变门的数值

### 任务：让门增加更多人数

**步骤：**

1. 打开 `GateManager.cs`
2. 找到 `Start()` 方法
3. 修改随机数范围：

```csharp
// 原代码：
randomNumber = Random.Range(10, 100);

// 修改为：
randomNumber = Random.Range(50, 200);  // 增加到50-200
```

4. 修改乘法门倍数：

```csharp
// 原代码：
randomNumber = Random.Range(1, 3);  // x1 或 x2

// 修改为：
randomNumber = Random.Range(2, 5);  // x2, x3, x4
```

5. 保存并测试

---

## ⚡ 第三个修改：改变游戏速度

### 任务：让游戏更快节奏

**步骤：**

1. 打开 `PlayerManager.cs`
2. 在 Inspector 中找到 PlayerManager 组件
3. 修改参数：

```
Player Speed: 5 → 10      (玩家移动速度加倍)
Road Speed: 5 → 10        (道路速度加倍)
Distance Factor: 1 → 0.7  (队伍更紧凑)
```

4. 或在代码中修改默认值：

```csharp
public float playerSpeed = 10f;  // 原来是 5f
public float roadSpeed = 10f;    // 原来是 5f
```

---

## 🏗️ 第四个修改：改变塔楼形状

### 任务：建造更高的塔楼

**步骤：**

1. 打开 `Tower.cs`
2. 找到 `BuildTower()` 方法
3. 修改参数：

```csharp
// 原代码：
int maxPlayerPerRow = 7;  // 每层最多7人

// 修改为：
int maxPlayerPerRow = 5;  // 每层最多5人（塔楼更高更窄）
// 或
int maxPlayerPerRow = 10; // 每层最多10人（塔楼更低更宽）
```

4. 修改间距：

```csharp
// 在 Inspector 中或代码中修改：
public float xGap = 0.5f;  // 原来是 1f，人更紧密
public float yGap = 1.5f;  // 原来是 1f，层更高
```

---

## 🎮 常用调试技巧

### 1. 查看实时人数
在 `PlayerManager.cs` 的 `Update()` 方法中添加：

```csharp
void Update()
{
    if (Input.GetKeyDown(KeyCode.Space))
    {
        Debug.Log($"当前人数: {numberOfStickmans}");
    }
}
```

### 2. 无敌模式
在 `PlayerManager.cs` 的 `EnemyAttack()` 协程中注释掉：

```csharp
// 注释掉这行，敌人不会减少玩家人数：
// numberOfStickmans--;
```

### 3. 快速增加人数
在 `PlayerManager.cs` 中添加：

```csharp
void Update()
{
    if (Input.GetKeyDown(KeyCode.A))
    {
        // 按A键增加50人
        AddStickMen(50);
    }
}

void AddStickMen(int count)
{
    for (int i = 0; i < count; i++)
    {
        GameObject obj = Instantiate(stickmanPrefab, player);
    }
    numberOfStickmans = player.childCount - 1;
    FormatStickMan();
}
```

### 4. 跳过战斗
在 `PlayerManager.cs` 的 `OnTriggerEnter()` 中：

```csharp
if (other.tag == "enemy")
{
    return;  // 直接返回，跳过战斗
}
```

---

## 📚 Inspector 快速参考

### PlayerManager 组件参数

| 参数 | 默认值 | 说明 | 建议范围 |
|------|--------|------|----------|
| Player Speed | 5 | 玩家横向移动速度 | 3-15 |
| Road Speed | 5 | 道路前进速度 | 3-15 |
| Distance Factor | 1 | 编队距离系数 | 0.5-2 |
| Radius | 1 | 螺旋编队角度 | 0.5-3 |
| Move By Touch | true | 启用触摸控制 | - |

### Tower 组件参数

| 参数 | 默认值 | 说明 | 建议范围 |
|------|--------|------|----------|
| X Gap | 1 | 水平间距 | 0.3-2 |
| Y Gap | 1 | 垂直间距 | 0.5-2 |
| Y Offset | 5 | 起始高度 | 0-10 |

---

## 🔧 常见问题快速解决

### Q: 游戏运行卡顿？
**A: 检查人数限制**
```csharp
// 在 PlayerManager.OnTriggerEnter() 添加：
if (numberOfStickmans > 200)
{
    numberOfStickmans = 200;  // 限制最大200人
}
```

### Q: 编队位置不对？
**A: 检查 Distance Factor 和 Radius 参数**
```
Distance Factor: 建议 0.7-1.5
Radius: 建议 0.8-1.5
```

### Q: 战斗太快/太慢？
**A: 修改战斗速度**
```csharp
// 在 EnemyAttack() 协程中修改：
yield return null;  // 每帧执行

// 改为：
yield return new WaitForSeconds(0.05f);  // 每0.05秒执行，变慢
```

### Q: 门的数字不显示？
**A: 检查 TextMesh Pro**
```
确保场景中有 EventSystem
确保 Canvas 的 Render Mode 正确
检查 Text 组件的 Font Asset
```

---

## 🎓 接下来学什么？

### 完成快速上手后，建议按此顺序深入学习：

**第1周：基础系统**
- ✅ 触摸控制原理
- ✅ 碰撞检测系统
- ✅ DOTween 动画库

**第2周：核心机制**
- ✅ 编队算法详解
- ✅ 战斗系统
- ✅ Coroutine 协程

**第3周：优化扩展**
- ✅ 对象池系统
- ✅ 关卡系统
- ✅ 配置数据分离

**第4周：高级功能**
- ✅ 事件系统
- ✅ UI系统
- ✅ 音效管理

---

## 💡 实用快捷键

### Unity 编辑器
```
F          - 聚焦选中对象
W/E/R      - 移动/旋转/缩放工具
Ctrl+D     - 复制对象
Ctrl+Shift+N - 新建空物体
Space      - 播放/暂停
Ctrl+P     - 播放游戏
```

### Visual Studio
```
Ctrl+K, Ctrl+D - 格式化代码
F12            - 转到定义
Ctrl+.         - 快速操作
Ctrl+/         - 注释/取消注释
Ctrl+Shift+F   - 全局搜索
```

---

## 📝 快速实验清单

尝试以下小实验来熟悉项目：

- [ ] 修改编队形状（圆形/方形/螺旋）
- [ ] 修改门的数值范围
- [ ] 修改游戏速度
- [ ] 修改塔楼参数
- [ ] 添加调试快捷键
- [ ] 修改动画缓动效果
- [ ] 修改战斗速度
- [ ] 修改敌人数量
- [ ] 添加新的 Tag 和碰撞
- [ ] 修改摄像机角度

---

## 🚀 下一步

完成快速上手后，查看：

1. **项目学习指南.md** - 完整的学习路线图
2. **优化代码示例/** - 参考优化代码
3. **Assets/Scripts/** - 阅读源代码并添加注释

**记住：**
- 🎯 先运行，再理解，最后修改
- 💡 每次只改一个地方，观察效果
- 📝 遇到问题先看 Debug.Log 输出
- 🔄 经常保存和备份代码

**祝你学习愉快！** 🎉

---

## 📞 获取帮助

- Unity 官方文档：https://docs.unity3d.com/
- DOTween 文档：http://dotween.demigiant.com/
- Unity 中文社区：https://unity.cn/
- 原教程视频：
  - Part 1: https://www.youtube.com/watch?v=dIdrsACbkgQ
  - Part 2: https://www.youtube.com/watch?v=WlFuYV2G4To
